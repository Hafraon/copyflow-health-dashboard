// ДОДАТКОВИЙ КОД - ТІЛЬКИ ЯКЩО ПОТРІБНА ІНТЕРАКТИВНІСТЬ\n// Цей файл НЕ ОБОВ'ЯЗКОВИЙ для базової функціональності alerts\n\nimport { NextResponse } from 'next/server'\nimport { telegramAlerts, sendTelegramAlert } from '@/lib/telegram-alerts'\nimport { getSystemHealth } from '@/lib/monitoring'\n\n// Webhook endpoint для отримання повідомлень від Telegram\nexport async function POST(request: Request) {\n  try {\n    const update = await request.json()\n    \n    // Перевірка що це повідомлення від користувача\n    if (!update.message || !update.message.text) {\n      return NextResponse.json({ ok: true })\n    }\n\n    const chatId = update.message.chat.id\n    const text = update.message.text\n    const userId = update.message.from.id\n\n    // Перевірка що повідомлення від правильного користувача\n    if (chatId.toString() !== process.env.TELEGRAM_CHAT_ID) {\n      return NextResponse.json({ ok: true }) // Ігноруємо\n    }\n\n    // Обробка команд\n    switch (text) {\n      case '/start':\n        await sendReply(chatId, `👋 Вітаю! Це CopyFlow Health Monitor Bot.\\n\\nДоступні команди:\\n/status - поточний статус системи\\n/health - детальна перевірка\\n/alerts - активні сповіщення`)\n        break\n\n      case '/status':\n        await handleStatusCommand(chatId)\n        break\n\n      case '/health':\n        await handleHealthCommand(chatId)\n        break\n\n      case '/alerts':\n        await handleAlertsCommand(chatId)\n        break\n\n      case '/help':\n        await sendReply(chatId, `🤖 CopyFlow Bot Commands:\\n\\n/status - Швидкий статус\\n/health - Детальна перевірка\\n/alerts - Список активних alerts\\n/help - Ця довідка`)\n        break\n\n      default:\n        // Невідома команда\n        if (text.startsWith('/')) {\n          await sendReply(chatId, `❓ Невідома команда: ${text}\\n\\nВикористайте /help для списку команд`)\n        }\n        break\n    }\n\n    return NextResponse.json({ ok: true })\n\n  } catch (error) {\n    console.error('Telegram webhook error:', error)\n    return NextResponse.json({ ok: true }) // Завжди відповідаємо ok\n  }\n}\n\n// Відправити відповідь користувачу\nasync function sendReply(chatId: number, text: string) {\n  try {\n    const response = await fetch(`https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendMessage`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        chat_id: chatId,\n        text: text,\n        parse_mode: 'HTML'\n      })\n    })\n    \n    return response.ok\n  } catch (error) {\n    console.error('Failed to send reply:', error)\n    return false\n  }\n}\n\n// Команда /status\nasync function handleStatusCommand(chatId: number) {\n  try {\n    const healthData = await getSystemHealth()\n    \n    const operationalServices = healthData.filter(s => s.status === 'operational').length\n    const totalServices = healthData.length\n    const healthScore = Math.round((operationalServices / totalServices) * 100)\n    \n    let message = `📊 <b>CopyFlow Status</b>\\n\\n`\n    message += `🎯 Health Score: <code>${healthScore}%</code>\\n`\n    message += `✅ Operational: <code>${operationalServices}/${totalServices}</code> services\\n\\n`\n    \n    message += `🔧 <b>Services:</b>\\n`\n    healthData.forEach(service => {\n      const emoji = service.status === 'operational' ? '✅' : \n                   service.status === 'degraded' ? '⚠️' : '❌'\n      message += `${emoji} ${service.service}\\n`\n    })\n    \n    await sendReply(chatId, message)\n    \n  } catch (error) {\n    await sendReply(chatId, `❌ Помилка отримання статусу: ${error instanceof Error ? error.message : 'Unknown error'}`)\n  }\n}\n\n// Команда /health\nasync function handleHealthCommand(chatId: number) {\n  await sendReply(chatId, `🔍 Запускаю детальну перевірку системи...`)\n  \n  try {\n    const healthData = await getSystemHealth()\n    \n    let message = `🔬 <b>Детальна перевірка здоров'я</b>\\n\\n`\n    \n    healthData.forEach(service => {\n      const statusEmoji = service.status === 'operational' ? '✅' : \n                         service.status === 'degraded' ? '⚠️' : '❌'\n      \n      message += `${statusEmoji} <b>${service.service}</b>\\n`\n      message += `   Status: ${service.status}\\n`\n      \n      if (service.responseTime) {\n        message += `   Response: ${service.responseTime}ms\\n`\n      }\n      \n      if (service.metadata?.error) {\n        message += `   Error: ${service.metadata.error}\\n`\n      }\n      \n      message += `\\n`\n    })\n    \n    await sendReply(chatId, message)\n    \n  } catch (error) {\n    await sendReply(chatId, `❌ Помилка health check: ${error instanceof Error ? error.message : 'Unknown error'}`)\n  }\n}\n\n// Команда /alerts\nasync function handleAlertsCommand(chatId: number) {\n  try {\n    // Тут можна підключити до бази alerts\n    await sendReply(chatId, `🚨 <b>Active Alerts</b>\\n\\nНаразі немає активних сповіщень\\n\\n💡 Alerts відправляються автоматично при виявленні проблем`)\n    \n  } catch (error) {\n    await sendReply(chatId, `❌ Помилка отримання alerts: ${error instanceof Error ? error.message : 'Unknown error'}`)\n  }\n}\n\n// Функція для налаштування webhook (викликається один раз)\nexport async function setupWebhook() {\n  const webhookUrl = `${process.env.RAILWAY_STATIC_URL}/api/telegram/webhook`\n  \n  try {\n    const response = await fetch(`https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/setWebhook`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        url: webhookUrl\n      })\n    })\n    \n    const result = await response.json()\n    console.log('Webhook setup result:', result)\n    \n    return result.ok\n  } catch (error) {\n    console.error('Failed to setup webhook:', error)\n    return false\n  }\n}\n